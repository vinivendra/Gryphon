Hello, world!
The string above is Hello, world!
0
Here's another interpolated string: Hello, world!, The string above is Hello, world! and 0
A string with "escaped double quotes" \ and escaped backslashes
	 and some escaped characters too.
Hello, world!
""Hello, world!
==

This is a multiline string.
It has many lines.

==
This multiline string has less whitespace.
It still has many lines.
==

		This multiline string has indentation.
		It also has many lines.

==

	This multiline string has nested indentation.
	And it has many lines.

==
i
a
b
c
a
b
c
$foo
${foo}
$foo
${foo} $foo
